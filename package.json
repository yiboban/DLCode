{
  "name": "dlcode",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n 后端,前端 -c blue,green \"npm run backend\" \"npm run frontend\"",
    "backend": "python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000",
    "frontend": "npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173",
    "test": "python -m pytest backend/tests -q && npm --prefix frontend run build",
    "install:all": "npm install && npm --prefix frontend install && python -m pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
